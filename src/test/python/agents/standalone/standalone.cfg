[Locations]
looks_config = standalone_looks.cfg
python_file = standalone.py
supports_standalone = True
name = StandaloneBot
maximum_tick_rate_preference = 120
requirements_file = requirements.txt
use_virtual_environment = True
